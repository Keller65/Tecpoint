/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['wikirock.net', "lh3.googleusercontent.com", "tecpoint.ws", "https://res.cloudinary.com"],
  },
};

export default nextConfig;
